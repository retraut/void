{
  "name": "void",
  "version": "0.1.0",
  "private": true,
  "description": "Best-in-class DX. Hetzner pricing. No SSH. Self-hosted edge-driven PaaS with MCP-native AI deploys.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:worker": "pnpm --filter @void/worker dev",
    "build:worker": "pnpm --filter @void/worker build",
    "build:agent": "pnpm --filter @void/agent build",
    "deploy:worker": "pnpm --filter @void/worker deploy",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22.10.0"
  }
}
